# HDR-Pipeline
Проект строит HDR-изображение из серии кадров с разной экспозицией (один нормальный, по два засвеченных и затемненных):
- выравнивание кадров методом **MTB (Median Threshold Bitmap)**
- калибровка отклика камеры **Debevec & Malik** (оценка `g(z)` по каналам RGB)
- HDR-слияние в радианс-карту
- тонмаппинг: **Reinhard**, **Drago**, **(упрощённый) Mantiuk**
- сохранение промежуточных и итоговых изображений (в репозитории приведены только оригиналы и результат)
Для работы используются библиотеки NumPy (вычисления и работа с массивами) и Pillow (чтение, запись картинок и простая постобработка). В связи с этим в коде нет OpenCV - все операции вроде выравнивания, слияний и всего прочего реализованы теми двумя библиотеками.
Более подробно (математика, логика и разбор) читайте в PDF-отчете
